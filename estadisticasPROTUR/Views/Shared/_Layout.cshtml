@using System.Web.UI.WebControls
<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Estadísticas PROTI</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/highcharts")
    @Styles.Render("~/Content/highcharts")
    @RenderSection("scripts", required: false)
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("estadísticas PROTUR", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Inicio", "Index", "Home")</li>
                    <li>@Html.ActionLink("Derivaciones", "Derivaciones", "Home")</li>
                    <li>@Html.ActionLink("Sobre..", "About", "Home")</li>
                    @{
                        /*List<SelectListItem> listItems = new List<SelectListItem>();


                        listItems.Add(new SelectListItem
                        {
                            Text = "Elija un submenú",
                            //Value = Url.Action("")
                        });
                        listItems.Add(new SelectListItem
                        {
                            Text = "Derivaciones",
                            Value = Url.Action("Derivaciones","Home")
                        });
                        */
                    }
                    @*@Html.DropDownList("ddNavegador", new SelectList(listItems, "Value", "Text"), htmlAttributes: new { @class = "form-control mainDropdown" })*@

                </ul>
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Área de Informática - Ministerio de Salud Pública de San Juan</p>
        </footer>
    </div>


    @Scripts.Render("~/bundles/user")
    @RenderSection("scripts", required: false)
</body>
</html>
